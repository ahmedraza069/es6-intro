const firstName = "Rakibul";
const middleName = "Hasan";
const lastName = "Raza";

const fullName = firstName + " " + middleName + " " + lastName;
console.log(fullName);

const a = 10;
const b = 20;

const result = "This values are sum " + a + " and " + b + " is " + (a + b);
console.log(result);

const math = `This result are ${a} and ${b} is ${a * b}`;
console.log(math);
// bektik super power

const policeDialouge = `He is the
 biggest 
criminal and he is the
 biggest busine
 ssman in t
 he world`;

console.log(policeDialouge);
